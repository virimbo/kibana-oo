name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Uncomment and configure for your stack:

      # --- Node.js ---
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '22'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test

      # --- Python ---
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.13'
      # - run: pip install -r requirements.txt
      # - run: python -m pytest

      # --- Go ---
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.23'
      # - run: go build ./...
      # - run: go test ./...

      - name: Placeholder
        run: echo "Configure CI for your stack"
